wish =

lets replace ptyxis with kitty

why? cause kitty supports remote control (and other features) that ptyxies does not 


most important aspects
- font
- theme
- keybinds

and also
- replace the default icon, to a custom one (or a better one non custom)

---

for now, lets just add it in parallel

we'll decide to cutover later if desired
