# execution: add kitty alongside ptyxis

## progress

- [x] 1. install kitty via apt — procedure written
- [x] 2. create kitty.conf with keybinds + remote control — procedure written
- [x] 3. create desert.conf theme — procedure written
- [x] 4. create and install custom icon — procedure written (awaited: custom icon file)
- [ ] 5. verify: run both terminals in parallel — awaited: human runs procedures

## implementation

### 1. install_kitty procedure

location: `src/install_env.pt4.terminal.kitty.sh`

```bash
source ~/git/more/dev-env-setup/src/install_env.pt4.terminal.kitty.sh
install_kitty
```

### 2. configure_kitty procedure

creates `~/.config/kitty/kitty.conf` with:
- font: Hack Nerd Font Mono 12
- remote control enabled
- ptyxis-parity keybinds

```bash
configure_kitty
```

### 3. configure_kitty_theme procedure

creates `~/.config/kitty/themes/desert.conf` with:
- desert palette colors

```bash
configure_kitty_theme
```

### 4. configure_kitty_icon procedure

creates custom icon and installs to .desktop file

prerequisites:
- add custom icon to `assets/kitty-icon.png` (256x256 px, png format)

```bash
configure_kitty_icon
```

### 5. verification

test remote control works:
```bash
kitty &
kitten @ ls
```

### files created

| file | purpose |
|------|---------|
| `src/install_env.pt4.terminal.kitty.sh` | kitty install and config procedures |
| `src/install_env._.sh` | updated dispatcher to include kitty |
| `src/bash_aliases.sh` | added `sync.devenv.kitty` alias |
| `assets/README.md` | instructions for custom icon |

### sync alias

after initial install, sync config changes via:
```bash
sync.devenv.kitty
```
