# criteria.blackbox.matrix: git backup

## matrix.1 = directory presence

covers: usecase.1, usecase.2, usecase.3

| ind: ~/git/ exists | ind: ~/.claude/ exists | dep: git.tar.gz | dep: .claude.tar.gz | dep: outcome |
|--------------------|------------------------|-----------------|---------------------|--------------|
| yes | yes | created | created | success with both archives |
| yes | no | created | skipped | success with warn |
| no | yes | n/a | n/a | error: no git dir found |
| no | no | n/a | n/a | error: no git dir found |

note: ~/.claude/ absence triggers warn, not error. ~/git/ absence is fatal.

## matrix.2 = argument validation

covers: usecase.4

| ind: --repo flag | ind: --into flag | dep: outcome |
|------------------|------------------|--------------|
| present | present | proceeds to backup |
| present | absent | error: --into required |
| absent | present | error: --repo required |
| absent | absent | error: --repo required |

note: --repo is checked first.

## matrix.3 = s3 interaction

covers: usecase.5, usecase.6, usecase.7

given: valid directories, valid arguments

| ind: s3 uri format | ind: aws credentials | dep: archives created | dep: upload | dep: outcome |
|--------------------|----------------------|-----------------------|-------------|--------------|
| valid (s3://...) | valid | yes | success | success with URIs + sizes |
| valid (s3://...) | invalid/expired | yes | fails | error from aws cli |
| invalid (no s3://) | any | no | n/a | error: invalid s3 uri |

## coverage verification

all combinations from blackbox criteria are represented:

- usecase.1 (happy path) → matrix.1 row 1
- usecase.2 (no git dir) → matrix.1 rows 3-4
- usecase.3 (no claude dir) → matrix.1 row 2
- usecase.4 (absent args) → matrix.2 all rows
- usecase.5 (invalid uri) → matrix.3 row 3
- usecase.6 (upload failure) → matrix.3 row 2
- usecase.7 (path structure) → implicit in matrix.3 row 1 success outcome

## gaps: none identified

all meaningful combinations are covered. no absent rows detected.

## decomposition: not needed

3 independent variables max per matrix. combinations are enumerable and reasonable. no signal to decompose further.
