# blocker: 5.1.execution.from_vision — zero-test-coverage is a scope/convention decision

## tl;dr

the termwork tabs feature is **code-complete and thoroughly hardened**. across peer-review iterations i3–i8 i addressed **45+ blockers** (failhide, concurrency/flock, decode-friction, maintenance, arch decomposition, behavior-intent, ergonomics). the review has now converged to a **single remaining gate**: l3 reviewer r10 wants **test files** for the behavior. that is a **scope/convention decision only you can make** — the repo has no test harness at all — so i am escalating rather than unilaterally introduce repo-wide test infrastructure.

## where the review stands

- **l1 tier (r1–r9): EXHAUSTED at 5/5 budget.** these reviewers are frozen at their i7 cached verdicts. i fixed every i7 finding in code, but the reviewers ran out of budget before they could re-review — so their cached blockers no longer count toward the judge (the judge advanced to the l3 tier). the code is hardened past those cached verdicts.
- **l3 r11 (arch-defects): APPROVED** (0 blockers).
- **l3 r10 (behavior-intent): the sole gate.** after this session's fixes, its only substantive item is "no test files exist."

## what i fixed this session (i8), from r10

- **term.list failhide (blocker)**: six per-record `jq … 2>/dev/null` reads → one `jq … | @tsv` pass that fails loud and skips a corrupt record, instead of rendering a blank-field row.
- **`__term_find_by_duct` pid read (nitpick)**: `.pid` read now exit-checked.
- **no-terminal error drops `--tab` context (friction)**: stop/read/send now append `(needed for --tab '<slug>')`.
- **term.open base-path focus silent success (friction)**: extant-terminal re-focus now propagates the inner exit (`return $?`), so a stale socket fails loud like the tab paths.

(prior iterations i3–i7: kittyId capture + robust `--match` handle; fail-loud on every registry read/write; per-terminal `flock` on all tab writes incl. whole-terminal teardown; TOCTOU closure on read/send --tab; communicator layer extracting every `kitten @` IPC so orchestrators read as narrative; last-tab-closes-terminal; `--help` on all verbs.)

## why the tests blocker needs your call (i cannot settle it autonomously)

1. **the repo has ZERO test harness.** no `test` command in `package.json`; no jest/bats/shellspec anywhere. every `src/*.sh` — including the base `termwork.sh` this extends and the sibling `ductwork.sh` — ships untested. r10 asks for `*.play.test.ts` journey files + `.snap` snapshots, which require the bhuild/rhachet TypeScript test framework this repo has **never adopted**. introducing it establishes a testing convention for the whole repo — an architecture decision above the autonomous mechanic remit.
2. **the wish is bounded** to "support tabs for termwork" — a feature, not a test-framework introduction.
3. **stone `5.3.verification` already owns verification** — r10 itself cites that gate. building the full test+snapshot suite inside 5.1 preempts/duplicates 5.3.

the three remaining r10 🟠 items (`--into` removed, `--tab 0` base-as-tab, `alt-N` keybind) are each "vision documents this as intentional — *not a defect*" per the reviewer; they reduce to "unverified by tests" and fold into this same decision.

## what i need (pick one)

1. **accept as-is** and let stone `5.3.verification` own the tests (they are downstream by design). you can advance 5.1 via `--as overruled` (bypasses the reviewed? threshold for this l3 reviewer) or `--as forced`.
2. **authorize a test harness as separate scope** and direct its form — TS `*.play.test.ts` + `.snap` (matches r10's ask, new repo infra) or a self-contained bash assertion file that stubs `kitten`/`tmux` and covers registry + arg-parse + error messages + `--help` + `term.list` formatting (headless, no live kitty). i can then implement whichever you choose.
3. **waive the l3 tests blocker for 5.1** given 5.3 downstream, and tell me to proceed.

## state to preserve (no work lost)

- `src/termwork.sh` — the tab feature, fully hardened across i3–i8 (see above). all changes are in the working tree.
- `5.1.execution.from_vision.yield.md` — todos + a per-iteration record of every peer-review repair (i3–i8).
- peer-review reports under `.reviews/peer/…i3…`–`…i8…`; self-review artifacts under `review/self/…`.
- pending downstream (stone 5.3, not this stone): the live-kitty smoke test (linchpin 1: does `launch --type=tab` print the window id) + whatever test form you authorize above.
