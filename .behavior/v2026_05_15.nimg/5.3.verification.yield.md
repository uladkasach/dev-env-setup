# verification: nimg

## verification checklist

### behavior coverage

this pr adds bash shell code only — no typescript, no test infrastructure.

| behavior | implementation | test? | verification |
|----------|----------------|-------|--------------|
| nimg command invokes pqiv | bash_aliases.sh:25-31 | n/a (bash) | manual: human confirmed "seems to work" |
| install_pqiv installs pqiv | install_env.pt4.terminal.sh:94-97 | n/a (bash) | idempotent apt call |
| configure_pqiv writes config | install_env.pt4.terminal.sh:99-118 | n/a (bash) | idempotent heredoc |

**why no automated tests:** this repo has zero test files for shell code. all install/configure procedures (install_vim, install_neovim, configure_neovim, etc.) are manually verified. to add tests for pqiv alone would be inconsistent with repo conventions.

**manual verification:** human confirmed in original session: "seems to work as is with the config from the wish".

### zero skips verified

- [x] no .skip() or .only() — no test files in scope
- [x] no silent credential bypasses — no credentials needed
- [x] no prior failures — no test infrastructure

### snapshot coverage for contract outputs

- [x] n/a — bash functions have no snapshot infrastructure in this repo

### tests executed

| test suite | applicable? | result |
|------------|-------------|--------|
| types | no (bash only) | n/a |
| lint | no (bash only) | n/a |
| format | no (bash only) | n/a |
| unit | no (bash only) | n/a |
| integration | no (bash only) | n/a |
| acceptance | no (bash only) | n/a |

**why n/a:** this pr touches only bash shell scripts. the repo has no test runner, no CI, and no test files for shell code. all install_* and configure_* procedures are manually invoked and verified by the human.

### contract output snapshot exhaustiveness

- [x] n/a — no snapshots for bash procedures in this repo

### blockers

none — bash procedures are manually verified; no automated verification required per repo conventions.

## summary

this pr adds:
1. `nimg` function to invoke pqiv with existence check
2. `install_pqiv()` to install pqiv via apt
3. `configure_pqiv()` to write vim keybindings config

all three are bash shell code. the repo has no test infrastructure for shell code. the human manually verified the implementation works: "seems to work as is with the config from the wish".

verification complete via manual test per repo conventions.
