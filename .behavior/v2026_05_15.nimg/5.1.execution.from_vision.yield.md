# execution: nimg

## todos

- [x] add `nimg` function to `src/bash_aliases.sh` (line 24-31)
- [x] add `install_pqiv()` procedure to `src/install_env.pt4.terminal.sh` (line 94-97)
- [x] add `configure_pqiv()` procedure to `src/install_env.pt4.terminal.sh` (line 99-118)
- [x] verified function and procedures are correct

## summary

added:
1. `nimg` function in bash_aliases.sh — checks for pqiv, emits helpful error if absent
2. `install_pqiv()` — installs pqiv via apt
3. `configure_pqiv()` — creates ~/.config/pqivrc with vim keybindings
