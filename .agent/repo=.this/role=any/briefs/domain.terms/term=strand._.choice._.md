# domain.term: strand

term.chosen   = strand
term.kind     = verb
term.synonyms.forbidden:
- orphan
- lose
- drop
- leak
- abandon

## .what
leave an account with no reachable copy of its refresh token — the failure a park exists to
prevent. a stranded account is recoverable only by a browser re-auth.

## .refs
where the term is declared / used:
- src/brains.auth.sh   # `_brains_auth_park_or_strand` — the fork the term names
- src/brains.auth.sh   # `_brains_auth_bak_strands`    — the guard that refuses a swap mid-strand
- .agent/repo=.this/role=any/skills/brains.auth.test.snap/swap.install-fails.strands.snap
- .agent/repo=.this/role=any/briefs/hazard.claude-oauth-refresh-rotation.md

## .reason
see the ref-level cluster beside this choice:
- `term=strand._.choice.reason.md` — etymology, the `orphan`/`lose` judgment, evidence, and the
  `park` / `strand` pair
