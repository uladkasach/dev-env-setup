dream = post the correction to `ehmpathy/rhachet` #482 — its evidence is wrong, and the two
defects it reports still stand

## ✔ .status = DISPATCHED — `ehmpathy/rhachet#494`, 2026-09-02

held from 2026-08-31 (`radio.uses get` → `global: blocked`) until the gate reopened. the body
posted is `.temp/correct482.md` **verbatim**, as this file instructed — not a recomposition.

🛑 **it is a task, and it wants to be a COMMENT on #482.** `gh issue comment` remains untested by
me, so the reachable transport was the one used. #494 opens with that ask: fold it onto #482 and
close it. until then the correction sits beside the wrong evidence rather than under it.

⚠️ **do not re-dispatch.**

## .the shape of it

the issue body cited an empty global root as proof a peer was absent:

```
~/.local/share/pnpm/global/5
dependencies:
  rhachet 1.47.2          ← and neither peer
```

that read came from `pnpm list -g --depth 0`, which lists **direct** dependencies only. pnpm
auto-installs peers, so a peer that loads perfectly well never appears in it. **the command did
not measure what the body said it did.**

two facts disprove the claim: a real `require('declastruct-aws')` from
`$(pnpm root -g)/rhachet/package.json` went green on that same box, and a later `keyrack set` got
PAST the peer check and failed one layer deeper, on aws identity — with no install in between.

## .what survives the correction

both hint-text defects are properties of the STRING, not of the box, so neither is touched:

1. `pnpm add` with no `-g` — rhachet finds its peers beside its own install, and for a global
   `rhx` that is the global root
2. one link of a three-link chain — `rhachet → declastruct-aws → declastruct`

## .the open question it leaves

the refusal fired on a box whose peer loads. so **rhachet's `set` path and a `require` from
rhachet's own `package.json` disagreed about one peer** — two readers over one question, free to
drift (`gotcha.a-check-that-cries-wolf-gets-silenced`, m.9). unmeasured; flagged, not asserted.

## .why a comment and never a quiet edit

so the wrong evidence does not vanish without a trace — the same reason this repo keeps 📜
correction blocks in place rather than a silent overwrite of the claim they correct.

## .see also

- `.temp/correct482.md` — the body to post, already written
- `.route/v2026_08_30.grove-provision-restructure/0.tasklist.md` — item E4
